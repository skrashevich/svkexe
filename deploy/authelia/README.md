# Legacy configuration

These files are retained from the original external-auth deployment. They are
not used by the current Compose stack. The gateway now uses its own login and
session cookie; see [the deployment guide](../../docs/DEPLOY.md).
