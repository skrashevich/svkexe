version: "3.9"

networks:
  svkexe:
    driver: bridge

services:
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    environment:
      DOMAIN: ${DOMAIN}
      ACME_EMAIL: ${ACME_EMAIL}
      CLOUDFLARE_API_TOKEN: ${CLOUDFLARE_API_TOKEN}
    networks:
      - svkexe

  authelia:
    image: authelia/authelia:latest
    restart: unless-stopped
    volumes:
      - ./authelia:/config
    environment:
      AUTHELIA_SESSION_DOMAIN: ${DOMAIN}
      AUTHELIA_SESSION_SECRET: ${AUTHELIA_SESSION_SECRET}
    networks:
      - svkexe

  gateway:
    build:
      context: ..
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL: ${DATABASE_URL}
      INCUS_ENDPOINT: ${INCUS_ENDPOINT}
      ENC_KEY: ${ENC_KEY}
      # The gateway has its own network namespace here, so a listener it opens
      # can never receive a VM's metadata request — and IP_FREEBIND means the
      # bind succeeds anyway, which would leave a healthy-looking log line in
      # front of a service nothing can reach.
      METADATA_ADDR: "off"
    depends_on:
      - caddy
      - authelia
    networks:
      - svkexe

  prometheus:
    image: prom/prometheus:latest
    restart: unless-stopped
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=30d"
    networks:
      - svkexe

  grafana:
    image: grafana/grafana:latest
    restart: unless-stopped
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
    volumes:
      - grafana_data:/var/lib/grafana
    depends_on:
      - prometheus
    networks:
      - svkexe

volumes:
  caddy_data:
  caddy_config:
  prometheus_data:
  grafana_data:
